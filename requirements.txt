# Core ML and Computer Vision
# Note: Install PyTorch manually first with the local wheel file
# torch==2.5.1+cu118  # Install manually from wheel
# torchvision  # Install after torch
# torchaudio   # Install after torch

# YOLO and Computer Vision - pinned for OBB compatibility
ultralytics  # Updated version for better compatibility
opencv-python==4.8.1.78
numpy==1.24.3
pillow==10.0.1

# Web Framework and UI
streamlit==1.28.1
streamlit-webrtc==0.47.1

# Utilities
gdown==4.7.1
av==10.0.0

# Optional dependencies for better performance
psutil
GPUtil

# Additional dependencies that might be needed
scipy
matplotlib
pandas

# Fallback dependencies for model loading issues
torch-model-archiver  # For handling different model formats
packaging  # For version parsing
requests  # For alternative model downloads
